<!-- ARCHIVADO 13-ago-2026 — describía 31 tests en 4 spec files y un CI de GitHub Actions que no existen; los usuarios de prueba y contraseñas eran falsos — sustituido por skill frontend-e2e-playwright + tests/e2e/fixtures/test-users.js -->
<!-- Origen: frontend/.claude/TESTING_GUIDE.md -->

# Guía de Testing E2E - Widdo

## Comandos de Testing

### Ejecutar Tests

```bash
# Ejecutar todos los tests E2E
npm run test:e2e

# Ejecutar tests con interfaz visual (recomendado para desarrollo)
npm run test:e2e:ui

# Ejecutar tests en modo debug (paso a paso)
npm run test:e2e:debug

# Ver reporte HTML de la última ejecución
npm run test:e2e:report
```

### Comandos Avanzados

```bash
# Ejecutar tests específicos
npx playwright test auth.spec.js
npx playwright test rbac.spec.js
npx playwright test players-crud.spec.js
npx playwright test multi-tenant.spec.js

# Ejecutar en navegador específico
npx playwright test --project=chromium
npx playwright test --project=firefox
npx playwright test --project=webkit

# Ejecutar tests en modo headed (ver navegador)
npx playwright test --headed

# Ejecutar con múltiples workers (más rápido)
npx playwright test --workers=3
```

## Suite de Tests Implementada

### 1. Autenticación (`auth.spec.js`)
**7 tests críticos:**
- ✅ Mostrar página de login
- ✅ Login con credenciales válidas
- ✅ Error con credenciales inválidas
- ✅ Validación de campos requeridos
- ✅ Logout exitoso
- ✅ Redirección a login sin autenticación
- ✅ Mostrar página de registro

### 2. RBAC (`rbac.spec.js`)
**6 tests de control de acceso:**
- ✅ Super Admin: acceso completo al sistema
- ✅ Propietario de club: gestión de jugadores/entrenadores/pagos
- ✅ Entrenador: solo categorías asignadas
- ✅ Jugador: sin acceso administrativo
- ✅ Padre: solo información de sus hijos
- ✅ Bloqueo de rutas sin permisos (403)

### 3. CRUD Crítico (`players-crud.spec.js`)
**11 tests de funcionalidad:**

**Jugadores:**
- ✅ Listar jugadores
- ✅ Crear nuevo jugador
- ✅ Editar jugador existente
- ✅ Eliminar jugador
- ✅ Validación de campos requeridos
- ✅ Búsqueda por nombre

**Pagos:**
- ✅ Listar cobros
- ✅ Registrar nuevo pago
- ✅ Validación de montos inválidos
- ✅ Cálculo correcto de saldo pendiente

### 4. Multi-tenant (`multi-tenant.spec.js`)
**7 tests de aislamiento:**
- ✅ Mostrar solo jugadores del club seleccionado
- ✅ Bloquear acceso cross-tenant por URL directa
- ✅ Persistir club seleccionado después de refresh
- ✅ Actualizar datos al cambiar de club
- ✅ Mostrar todos los clubes del usuario en selector
- ✅ Filtrar búsquedas por club activo
- ✅ Manejar usuario sin acceso a clubs

## Pre-requisitos para Ejecutar Tests

### 1. Backend en Ejecución
```bash
cd /Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport
docker compose up -d
```

### 2. Frontend en Ejecución
```bash
cd /Users/miguelcano/Desktop/todo/Widdo/desarrollo/frontend
npm run dev
```

### 3. Datos de Prueba
Ejecutar seeders en el backend:
```bash
docker compose exec saas_sport_app php artisan setup:realistic-data
```

## Usuarios de Prueba

| Rol | Email | Password |
|-----|-------|----------|
| Super Admin | `admin@sportsclub.co` | `admin123` |
| Propietario Club | `juan.ramirez@example.com` | `password123` |
| Entrenador | (ver seeders) | `password123` |
| Jugador | (ver seeders) | `password123` |
| Padre | (ver seeders) | `password123` |

## Estructura de Tests

```
frontend/
├── playwright.config.js          # Configuración de Playwright
├── tests/
│   └── e2e/
│       ├── auth.spec.js          # Tests de autenticación
│       ├── rbac.spec.js          # Tests de permisos
│       ├── players-crud.spec.js  # Tests de CRUD
│       ├── multi-tenant.spec.js  # Tests de aislamiento
│       └── README.md             # Documentación detallada
└── package.json                  # Scripts npm
```

## Configuración de Playwright

**Ubicación:** `playwright.config.js`

**Configuración actual:**
- ✅ Tests en: `./tests/e2e`
- ✅ Ejecución paralela activada
- ✅ 2 reintentos en CI
- ✅ Base URL: `http://localhost:5173`
- ✅ Screenshots en fallos
- ✅ Videos en fallos
- ✅ Trace on first retry
- ✅ 3 navegadores: Chromium, Firefox, Webkit

## Debugging de Tests

### Ver ejecución en tiempo real
```bash
npm run test:e2e -- --headed
```

### Pausar en fallos
```bash
npx playwright test --headed --pause-on-failure
```

### Debug paso a paso
```bash
npm run test:e2e:debug
```

### Inspeccionar elementos
```bash
npx playwright codegen http://localhost:5173
```

## CI/CD

Los tests están listos para CI/CD:
- ✅ Reintentos automáticos (2 veces)
- ✅ Workers secuenciales en CI
- ✅ Screenshots y videos de fallos
- ✅ Reporte HTML generado
- ✅ Configuración para GitHub Actions/GitLab CI

### Ejemplo GitHub Actions
```yaml
- name: Install dependencies
  run: npm ci

- name: Install Playwright Browsers
  run: npx playwright install --with-deps

- name: Run Playwright tests
  run: npm run test:e2e

- name: Upload test results
  if: always()
  uses: actions/upload-artifact@v3
  with:
    name: playwright-report
    path: playwright-report/
```

## Próximos Pasos

1. **Ejecutar tests**: `npm run test:e2e:ui`
2. **Verificar resultados**: Todos los tests deben pasar
3. **Corregir bugs**: Si algún test falla, corregir el código
4. **Documentar hallazgos**: Actualizar PROGRESS.md con resultados

## Cobertura Actual

**Total de tests:** 31 tests
- ✅ Autenticación: 7 tests
- ✅ RBAC: 6 tests
- ✅ CRUD: 11 tests
- ✅ Multi-tenant: 7 tests

**Funcionalidades cubiertas:**
- Login/Logout
- Control de acceso por roles
- CRUD de jugadores
- Registro de pagos
- Aislamiento de datos entre clubes
- Validaciones de formularios
- Búsquedas y filtros

---

**Última actualización:** 2025-11-07
**Versión Playwright:** @playwright/test v1.50+
