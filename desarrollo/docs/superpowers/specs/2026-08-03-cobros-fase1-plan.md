# Plan de ejecución — Fase 1 de Cobros v2

> ## ✅ SUPERSEDED (13-ago-2026) — ejecutado por la vía del spec padre
>
> Este plan quedó **obsoleto**: lo que describe se ejecutó dentro de
> **`2026-07-30-cobros-v2-revenue-assurance.md`** el 5-ago y está **en producción**
> (commits `5837ab0` / `cff1d41a`, 2214 tests verdes). El estado 🟡 de abajo es de antes de
> ese cierre; **no lo tomes como pendiente**.
>
> Verificado el 13-ago contra el código: `covers_from` aparece en **61** archivos de
> `saas_sport/` y `payment_modality` en **48**. Es decir, D1/D2 no siguen abiertas: se
> implementaron.
>
> Las preguntas **D1-D6 (§ final)** se cerraron como **P1-P9** en el spec padre. Para el
> estado real, la tabla de fases y lo que queda vivo, **lee el spec padre, no este archivo**.
>
> 🔴 Sí siguen abiertos, pero anotados en el spec padre: el backfill de ABA no llama a los
> guards, y los trimestres marcados «Pago Único» quedan sin periodo.

**Spec padre:** `2026-07-30-cobros-v2-revenue-assurance.md`
**Fecha:** 2026-08-03
**Estado (histórico, 3-ago):** 🟡 **EN EJECUCIÓN.** Los pasos 2, 3 y 4 (arreglos previos `1.0.a/b/c`) están
escritos y commiteados en la rama `fase1/arreglos-previos` (`b716b7a`), **sin verificar ni
pushear**: Docker dejó de responder antes de poder correr la suite. Del paso 5 en adelante
sigue esperando las decisiones D1-D6.

> Los arreglos previos NO dependen de ninguna decisión — por eso se hicieron primero.

---

## 📋 Plan de ejecución de la Fase 1 (3-ago-2026)

Levantado con 5 agentes en paralelo sobre el código + medición directa en producción.
**Verificado a mano por el orquestador**: los hallazgos marcados ✅ abajo se comprobaron leyendo
el código y consultando la BD de producción, no se aceptaron por reporte.

### Veredicto

La Fase 1 NO se puede ejecutar tal como está escrita: hay que reordenarla y meterle tres arreglos previos. Primero, el punto de partida está mal: el árbol `saas_sport/` (HEAD 6e4fcaa) va 10 commits por detrás de `origin/main` (25b24ea) y `git log origin/main..HEAD` está vacío — es fast-forward limpio, pero arrancar donde está hoy reintroduce los 5 bugs de deuda ya corregidos y vuelve a resolver B1 (verifiqué que la migración `2026_08_04_000100_sync_is_canceled_with_status.php` solo existe en origin/main). Segundo, el orden 1.1→1.5 ignora que el backfill de 1.2 se COME el `due_date`, y hoy `due_date` lo producen cuatro fórmulas distintas, dos de las cuales empujan el vencimiento al mes siguiente (`ChargeService.php:654`, `PlaClubTeamChargeController.php:839`): si backfilleas antes de arreglarlas, congelas el mes equivocado en un campo que después nadie va a revisar. Tercero, 1.1 no es "una columna": son 5 escritores, 5 seeders y una cascada de 10 reglas donde la frecuencia MIENTE (ABA registró sus trimestres como 'Pago Único' — `AbaClubSeeder.php:428`), así que clasificar por frecuencia manda los trimestres reales a 'other' y las matrículas a 'training_fee'. Cuarto, 1.2 no es un generador sino once, y diez de ellos dejarían `covers_*` en NULL si solo tocas el cron. Quinto, 1.3 son dos tareas distintas que conviene separar: los cubos de dinero (no dependen de 1.1 ni 1.2 y pueden empezar ya) y el predicado "¿está cubierto?" (depende de las dos). Sexto, 1.4 nace con 3 de los 4 motivos del spec: "sin modalidad" necesita `payment_modality`, que no existe en el código (grep vacío en app/ y database/), así que o sube 2.1 a Fase 1 o el reporte sale con 3 motivos y se documenta. Séptimo, 1.5 no puede vivir dentro del daily brief: el brief se calcula cuando alguien abre el home dentro de un caché de 15 minutos y no persiste nada, así que por sí solo nunca habría cazado a ABA en marzo — hace falta cron + fila persistida, copiando `PlatformSignalService`. Y el fallo silencioso más probable de toda la fase es de frontend: `OwnerTodayHome.jsx:182` filtra los items contra la lista blanca `WARNING_META`, y hay dos señales vivas (`stale_incapacity`, `no_proof_uploaded`) que llevan meses construyéndose en backend sin renderizarse en la web.

### Orden de ejecución

| # | Tarea | Riesgo | Qué hace |
|---|---|---|---|
| 1 | `1.0 (previo)` | bajo | Fijar el punto de partida en origin/main. VERIFICADO: HEAD local = 6e4fcaa, origin/main = 25b24ea, `git log origin/main..HEAD` VACIO -> HEAD es ancestro, fast-forward limpio, no hay nada local que perder. Sin esto la Fase 1 nace sin 25b24ea (un cobro borrado deja de contar como deuda en otros 5 sitios), sin 3e7e20f (retirar el borrado), sin 0840e5a (revivir la mora) y sin la migracion 2026_08_04_000100_sync_is_canceled_with_status.php, que SOLO existe en origin/main. RESUELVE CONTRADICCION: el frente 'cobertura' reporto PaymentStatsService.php:262 como trampa de soft delete; en origin/main esa consulta YA filtra p.deleted_at (:262) y pl.deleted_at (:263). Leyo codigo atrasado. Tiene razon el frente 'metricas'. La trampa que SI sigue viva en origin/main es ReadTools.php:1655 (solo pl.deleted_at, alias p = pagos sin filtrar). |
| 2 | `1.0.a (previo, bloquea 1.2)` | medio | Sustituir los dos whereIn de estados escritos a mano en el lado de la ESCRITURA por PlaClubTeamPayment::ACTIVE_STATUSES. VERIFICADO: el dispatch 'generar pagos ahora' usa ['PEN','PAR','COM'] (PlaClubTeamChargeController.php:739) y el generador de cuota de torneo usa ['PEN','PAR','COM','OVD','PEV'] (PlaTournamentController.php:3518); ninguno incluye AGR ni EXO. Efecto en prod HOY: a un jugador con acuerdo de pago o exonerado se le crea un SEGUNDO cobro del mismo mes. Va antes de 1.2 porque con covers_* eso deja de ser un duplicado invisible y pasa a ser dos filas con el mismo covers_from; y hace imposible cualquier indice unico posterior. Es la misma trampa DEBT_STATUSES/ACTIVE_STATUSES del spec, pero del lado de la escritura. |
| 3 | `1.0.b (previo, bloquea 1.1)` | bajo | Arreglar el mapa de frecuencias antes de que la migracion retro lo herede. VERIFICADO contra BasFrequencySeeder.php:15 (orden: 1 Pago Unico, 2 Mensual, 3 Trimestral, 4 Semestral, 5 Anual): PlaClubTeamChargeController.php:839 hace `frequency_id != 5 // 5 = Pago Unico` cuando 5 es ANUAL, y DispatchChargeDialog.jsx:116 y :178 repiten el mismo literal; ademas BasFrequency::FREQUENCY_ANNUAL=3 apunta a Trimestral (BasFrequency.php:14, hoy inerte porque getFrequencyDescription() no se llama en ningun sitio). Regla de la casa a partir de aqui: la recurrencia se decide SIEMPRE por bas_frequencies.is_single_payment / interval_type / interval_count, NUNCA por id ni por constantes del modelo. |
| 4 | `1.0.c (previo, BLOQUEA el backfill de 1.2)` | medio | Unificar el calculo de due_date para que ninguna ruta salte de mes. VERIFICADO: cuatro formulas distintas. El cron (GenerateMonthlyPayments.php:534) y el observer (PlaClubTeamPlayerObserver.php:338) acotan a fin de mes ACTUAL (correcto). ChargeService.php:654 y PlaClubTeamChargeController.php:825 hacen addMonth() si el due_day ya paso -> el pago del mes N queda con due_date del mes N+1. Y el propio dispatch comprueba el duplicado por whereYear/whereMonth(due_date, referenceDate) (:744) contra un due_date que ya salto (:840): generar el 28 crea el pago de agosto venciendo en septiembre y luego septiembre se salta. ESTE ES EL PASO QUE MAS REORDENA LA FASE: el backfill de covers_* (paso 10) lee due_date; si se hace antes de arreglar esto, congela el mes equivocado en un campo que nadie va a volver a mirar. |
| 5 | `1.1.a` | bajo | Migracion aditiva: `concept` varchar(20) NULLABLE (NO enum: el spec ya prevé un sexto valor 'agreement' y ampliar un ENUM en MySQL es un ALTER con reescritura; el precedente correcto en la tabla es `apply_to` varchar(20), migracion 2026_02_18_000001) + `concept_needs_review` boolean default 0. Anadir 'concept' a $fillable (PlaClubTeamCharge.php:36) — obligatorio, no cosmetico: sin fillable, `create($request->validated())` lo descarta EN SILENCIO, exactamente como hoy desaparecen late_fee_percentage/late_fee_amount/late_fee_grace_days que el agente IA cree que configura (WriteTools.php:417 escribe columnas que no existen). No anadir cast (es string). Observer/mutator que mantenga is_enrollment_fee = (concept === 'enrollment') como espejo, para que los 9 lectores del booleano (ChargeService::isChargeProratable:288, detectDuplicateEnrollmentFees:1135, PublicEnrollmentController:1808, SimpleDashboardController:994, ClubExportService:350, badges del frontend...) NO haya que tocarlos en Fase 1. |
| 6 | `1.1.b` | bajo | Que los 5 caminos de creacion escriban concept, ANTES del backfill, o los cobros nuevos nacen sin clasificar mientras el backfill corre. VERIFICADO, inventario cerrado: (1) web/API via StorePlaClubTeamChargeRequest.php:100 + UpdatePlaClubTeamChargeRequest.php:108 (validacion required/in:...) alimentado por useChargeWizard.jsx:602; (2) PlaTournamentController.php:3476 -> concept='event'; (3) WriteTools.php:422 (agente IA de chat) + ToolDefinitions.php:373 (schema del tool); (4) OnboardingToolExecutor.php:433; (5) PaymentAgreementService.php:52 -> concept='agreement'. Mas factory y 5 seeders, TRES de ellos con DB::table()->insertGetId (MultiRoleUserDataSeeder.php:432/608/617) que revientan si la columna nace NOT NULL. Arreglar de paso el late_fee_* fantasma del agente: es el mismo mecanismo y es un bug real del camino que mas cobros crea sin supervision humana. NO TOCAR: ChargesSheetContent.jsx (1362 lineas con UI de is_enrollment_fee en :1210-1259) esta MUERTO, no lo importa nadie. |
| 7 | `1.1.c` | alto | Clasificacion retro por CASCADA ordenada, primera regla que acierta gana, ejecutada primero como comando --dry-run que imprime el reparto y la lista de dudosos, y solo despues como migracion idempotente. Orden OBLIGATORIO (la frecuencia es la ULTIMA senal, no la primera): R0 name='__payment_agreement__' -> agreement; R1 id en pla_tournaments.charge_id o name LIKE 'Torneo:%' -> event; R2 is_enrollment_fee=1 -> enrollment; R3 billing_mode='per_attendance' -> training_fee; R4 nombre uniforme/kit/dotacion -> uniform; R5 nombre torneo/campeonato/viaje/clinic -> event; R6 nombre matricula/inscripcion -> enrollment; R7 nombre mensualidad/cuota/trimestre/semestre -> training_fee; R8 frecuencia con is_single_payment=0 e interval_type='months' -> training_fee; R9 resto -> concept='other' + concept_needs_review=1. POR QUE ESTE ORDEN: verificado en AbaClubSeeder.php:428/435 (espejo de produccion, mapea prod_charge_id) que ABA registro 'Trimestre Categorias Sub 13-18' con frequency 'Pago Unico' y sus matriculas con 'Anual'; la regla del spec 'recurrente mensual -> training_fee, resto -> other' manda los trimestres REALES al cubo equivocado. Correr las regex sobre stripDeletedSuffix (los borrados llevan name_deleted_<ts>) e incluir soft-deleted. |
| 8 | `1.1.d` | medio | Cerrar la columna: NOT NULL DEFAULT 'other' SOLO cuando SELECT COUNT(*) WHERE concept IS NULL = 0. Migracion aparte, tercera. Nunca en el mismo ALTER que la creacion: en MySQL sobre una tabla con filas eso exige que el backfill ya este hecho, y es justo donde revienta si una regla devuelve NULL. Mantener concept_needs_review como el estado 'sin clasificar' distinto de 'other': si todo lo dudoso cae en 'other', 'other' pierde significado, el aviso 'clasifica tus cobros' no sabe cuando apagarse y el reporte de 1.4 no puede distinguir 'es otro' de 'no lo hemos mirado'. |
| 9 | `1.2.a` | medio | Columnas covers_from / covers_to (date, nullable) + covers_reason (varchar, para distinguir 'no aplica' de 'desconocido'), y derivarlas en el hook `creating` de PlaClubTeamPayment.php:23 — que es el UNICO punto por el que pasan los ONCE creadores (ahi ya se resuelve el consecutivo). VERIFICADO: son 11 sitios en produccion, no 5: cron (GenerateMonthlyPayments.php:560), alta de jugador (ChargeService.php:239), observer y cambio de categoria (PlaClubTeamPlayerObserver.php:280 + PlayerService.php:1393), dispatch (PlaClubTeamChargeController.php:845), registro manual (PlaClubTeamPaymentController.php:473), inscripcion publica (PublicEnrollmentController.php:2075), torneo (PlaTournamentController.php:3529), asistencia (AttendancePaymentService.php:112), agente IA (WriteTools.php:971), cuotas de acuerdo (PaymentAgreementService.php:155), backfill (BackfillMissingMonthlyCharges.php:471). Los que SI conocen el periodo lo pasan explicito: cron (mes del club en su timezone), alta prorrateada (covers_from = fecha de INGRESO real, no dia 1 — es la unica forma de que 'monto != mes completo' quede explicado en el dato), dispatch con targetMonth, asistencia (covers_from = covers_to = attendance_date). Extraer la ventana de periodo por frecuencia de GenerateMonthlyPayments::getExistingPayment:465-517 a un helper unico periodo(charge, fecha) -> [from,to]: YA esta bien escrita y ya usa ACTIVE_STATUSES; 1.3 debe reusarla, no reescribirla. |
| 10 | `1.2.b` | alto | Backfill de covers_* con CUATRO reglas y una segunda pasada, no con la regla unica del spec. Regla 1 (mensualidades reales, la unica automatizable): p.covers_from=DATE_FORMAT(due_date,'%Y-%m-01'), covers_to=LAST_DAY(due_date), acotada a f.interval_type='months' AND f.interval_count=1 AND f.is_single_payment=0 AND billing_mode<>'per_attendance' AND is_enrollment_fee=0 AND attendance_id IS NULL AND agreement_role IS NULL AND c.specific_due_date IS NULL. Regla 2: attendance_id NOT NULL -> covers_from=covers_to=attendance_date. Regla 3: matriculas y pagos unicos -> NULL con covers_reason='temporada no modelada' / 'no aplica' (la temporada es Fase 3, §5.4). Regla 4 (SEGUNDA PASADA, despues de la primera): cuotas de acuerdo (agreement_role='installment') heredan min(covers_from)/max(covers_to) de los covered — y solo es seguro si el reporte excluye concept='agreement' en la consulta canonica, no en cada reporte, o el mes queda cubierto DOS veces (la fila AGR original conserva su periodo). EXCLUIR de la regla 1 los pagos con mes(due_date) != mes(created_at): son los que produjeron las formulas rotas del paso 4 y quedarian en un mes ya cubierto. Decision explicita a escribir, no dejar al azar: los soft-deleted SI se backfillean (son historia; el reporte los excluye por su cuenta). |
| 11 | `1.3.a (puede ir en paralelo desde el paso 1)` | alto | ClubMetricsService con los cubos de DINERO. No depende de 1.1 ni de 1.2: puede arrancar en paralelo. VERIFICADO: 'recaudado' no tiene 3 definiciones vivas sino OCHO, sobre 4 columnas de fecha (reviewed_at, paid_at, payment_date y updated_at) y 2 fuentes de importe (installments APR vs payments.amount_paid): PaymentStatsService.php:59, FinancialReportController.php:228, :352, PlaClubTeamController.php:479, AdvancedTools.php:495, DailyBriefService.php:236, SendWeeklyClubReport.php:58, ReadTools.php:1187. Requisitos de forma: `periodo` y `base` (devengo/caja) como parametros OBLIGATORIOS SIN default (todo el desastre actual son defaults silenciosos: getTotalBilled:144 no acepta periodo y getReceivedAmount:59 si, asi que la cabecera de Pagos pinta una caja mensual junto a una deuda historica y I2 hoy ni siquiera es evaluable); TODOS los cubos derivados de UNA tabla de decision estado x cubo escrita como constante de datos, para que sea imposible que dos metodos discrepen (hoy getTotalBilled excluye AGR/EXO y getReceivedAmount no, asi que su dinero cobrado sigue en el recaudo); baseQuery() privado que use withoutGlobalScope(ClubScope::class) y NUNCA withoutGlobalScopes() sin argumentos (que quita tambien el SoftDeletingScope y ya fallo en 6 sitios); I2 con termino propio para el RECARGO POR MORA desde el primer diseno (facturado + recargos = recaudado + por_cobrar + anulado), porque la mora reescribe remaining_amount sin tocar amount_due y el dia que reviva sobre los 586 vencidos descuadra I2 en todos los clubes a la vez; y B1 ya esta cerrado en origin/main (hook is_canceled = status==='CXL' + migracion 2026_08_04_000100), asi que la consulta canonica mira SOLO status y no arrastra el booleano. Adopcion por DELEGACION: PaymentStatsService conserva su firma publica y por dentro llama al nuevo servicio (los 11 llamadores se benefician sin tocarlos). |
| 12 | `1.3.b` | alto | Predicado canonico 'esta cubierto?' + universo canonico de 'jugador activo'. VERIFICADO: hay TRES definiciones incompatibles de 'ya existe pago de este periodo' y las tres usan ACTIVE_STATUSES (bien) pero responden distinto: ChargeService.php:174 no tiene NINGUNA ventana temporal (un pago activo de cualquier epoca lo da por cubierto para siempre), GenerateMonthlyPayments.php:465 usa ventana por due_date segun la frecuencia, PlaClubTeamPaymentController.php:275 usa ventana por created_at. Y hay CUATRO definiciones de 'jugador activo' (PlaClubTeamPlayer NO tiene scope active): status='ACT'; withTrashed()+status!='BOR'; deleted_at IS NULL AND end_date IS NULL AND users.status!='BOR'; deleted_at IS NULL AND status!='BOR'. Fijar UNA (recomendado: status='ACT' + SoftDeletes por defecto via Eloquent + whereHas('categories')) y que las tres implementaciones pasen a llamar al servicio. Ojo con per_attendance: hay DOS predicados incompatibles (el generador y ChargeService/PlayerService/backfill usan whereHas('players'), AttendancePaymentService.php:175 usa appliesToPlayer() con apply_to); el reporte debe usar EL DEL GENERADOR o dira 'sin cuota' de alguien a quien el cron nunca le va a generar nada. |
| 13 | `1.3.c` | medio | Cerrar las fugas que quedan y poner el trinquete, o el servicio canonico convive con los literales y no cambia nada (que es exactamente lo que le paso a PaymentStatsService: existe hace meses, esta bien documentado, y aun asi conviven 7 definiciones mas de recaudado). CUATRO fugas de soft delete VERIFICADAS EN origin/main (no en el arbol atrasado): ReadTools.php:1655 getDebtorsSummary — alias p=pagos, pl=jugadores, tiene whereNull('pl.deleted_at') y NO p.deleted_at: es EXACTAMENTE el patron trampa del encargo, y es la lista de deudores que el agente le lee al director; ReadTools::getPendingPayments — la lista (desde :259) sin deleted_at contra sus totales que si filtran, en la MISMA respuesta; FamilyTools lista de pagos de la familia (sin deleted_at) vs sus totales (:314, si filtran) — un padre ve un cobro que el club borro; PlaClubTeamChargeController::playerChargeDetail:1223 con withoutGlobalScopes() sin whereNull. Y CUATRO whereIn de estados a mano en el agente: AdvancedTools.php:509/:725/:745 y PlayerTools.php:71 en SQL crudo; el peor es AdvancedTools.php:779 (queryActiveGoodStanding) que filtra ['OVD','PEN'] SIN 'PAR' -> quien abono a medias se reporta al director como 'al dia'. Trinquete estatico (el repo ya tiene el mecanismo, api:contract-check con baseline en cero) que prohiba DB::table('pla_club_teams_payments') y whereIn(...'status'...) fuera del namespace de metricas, baseline congelado y prohibicion de crecer. |
| 14 | `1.4` | bajo | Reporte de cobertura con motivos. Solo lectura, riesgo bajo, pero solo tiene sentido despues de 1.1+1.2+1.3.b. Endpoint GET /api/pla_club_teams/{club}/financial-reports/coverage dentro del grupo existente (routes/api.php:1072, middleware module.access:reports), con authorize() EXPLICITO — el controlador actual comprueba roles a mano y no tiene Policy, y la regla de la casa para endpoints nuevos es authorize(). UN solo endpoint que devuelva tiles + filas, para poder auto-verificar I3 sobre su propia respuesta. Universo: jugador ACT + con categoria (via relacion Eloquent, NO leftJoin sobre la pivote: pla_club_teams_category_player no tiene club_id ni deleted_at ni status, y un jugador cuya unica categoria esta BORRADA cuenta como 'con categoria' si se hace con DB::table) + no excluido + no per_attendance (predicado del GENERADOR) + no exento total. Pagos considerados: concept='training_fee' AND agreement_role IS NULL, en ACTIVE_STATUSES. Cuatro motivos con PRECEDENCIA escrita y testeada (sugerido 4<1<3<2, porque la maqueta M3 pinta a Helen como 'cancelado sin reemplazo' teniendo modalidad sin definir): (1) nunca facturado — mira TODA la historia incluyendo CXL y soft-deleted, o un cancelado se clasifica mal aqui; (2) periodo vencido — necesita covers_*; (3) cancelado sin reemplazo — los datos YA existen y son fiables de aqui en adelante (cancelPayment escribe canceled_at/canceled_by/cancellation_reason y payments:clean-deleted esta COMENTADO en el scheduler, verificado en routes/console.php:165, ademas con candado de dinero: amount_paid>0 nunca se destruye), pero NO para los 172 CXL historicos que solo tienen el motivo en comments; (4) sin modalidad — NO TIENE DATOS en Fase 1. Cuarto cubo explicito 'periodo desconocido' que no cuenta ni como cubierto ni como fuga. Bloque HERMANO y rotulado aparte 'activos que nunca han pagado' (predicado de doble senal: SUM(amount_paid)=0 Y ningun installment APR, porque bulkMarkPaid pone COM sin installment y los pagos legacy no tienen installments), que NUNCA suma al tile de fuga o I3 se rompe. Frontend: 4a pestana en FinancialReportsPage.jsx:551, claves i18n financial.* en el namespace payments (que es DEFAULT en esa pagina). |
| 15 | `1.5` | medio | Senal persistente + aviso en inicio. El daily brief NO es el detector: DailyBriefService.php:29 recalcula 12 items en memoria dentro de un Cache::remember de 15 min por club+locale y no persiste NADA (el unico estado es AIAutonomousAction.reported_at:199); se calcula cuando alguien abre el home, asi que por si solo nunca habria cazado a ABA en marzo. Copiar el patron que SI tiene cron, persistencia y ciclo de vida: PlatformSignalService::persist:239 (idempotente, conserva detected_at si la condicion sigue, marca resolved si desaparece) + cron en routes/console.php:216. Comando revenue:detect-coverage-gaps a las 07:00, DESPUES de app:generate-monthly-payments, con withoutOverlapping()->onOneServer(); tabla propia con status active/resolved, count, reasons[], detected_at, last_notified_at, snoozed_until; umbrales en config/revenue_assurance.php (precedente explicito: config/platform_signals.php); escalado por ANTIGUEDAD del periodo vencido, que es lo que dispara en marzo sin esperar a volumen. El brief solo LEE la fila. FALLO SILENCIOSO NUMERO UNO: OwnerTodayHome.jsx:182 filtra los items contra la lista blanca WARNING_META (:63, 4 tipos); stale_incapacity y no_proof_uploaded llevan meses construyendose con severity 'warning' en backend SIN renderizarse en ninguna parte de la web — grep de esos tipos en frontend/src no devuelve nada. En movil SI se veria (filtra por severity, mobile/app/(app)/(tabs)/index.jsx:131), asi que el QA movil puede dar verde con la web muerta. Anadir tambien el caso al match de sugerencia (:316 en, :323 es) o la senal no tendra call-to-action. NADA de catch mudo: las 4 anomalias actuales terminan en '// Silently skip' (:248, :265, :280, :296) y la anomalia de recaudo estuvo muerta meses sin que nadie lo notara. Arreglar de paso esa anomalia, que usa withoutGlobalScopes() sin whereNull('deleted_at') (suma pagos borrados) y compara por updated_at (DailyBriefService.php:236-243) — que ademas se movera en masa el dia que reviva la mora. |

#### Detalle por paso

**1. 1.0 (previo)** (riesgo bajo, depende de: nada)

Fijar el punto de partida en origin/main. VERIFICADO: HEAD local = 6e4fcaa, origin/main = 25b24ea, `git log origin/main..HEAD` VACIO -> HEAD es ancestro, fast-forward limpio, no hay nada local que perder. Sin esto la Fase 1 nace sin 25b24ea (un cobro borrado deja de contar como deuda en otros 5 sitios), sin 3e7e20f (retirar el borrado), sin 0840e5a (revivir la mora) y sin la migracion 2026_08_04_000100_sync_is_canceled_with_status.php, que SOLO existe en origin/main. RESUELVE CONTRADICCION: el frente 'cobertura' reporto PaymentStatsService.php:262 como trampa de soft delete; en origin/main esa consulta YA filtra p.deleted_at (:262) y pl.deleted_at (:263). Leyo codigo atrasado. Tiene razon el frente 'metricas'. La trampa que SI sigue viva en origin/main es ReadTools.php:1655 (solo pl.deleted_at, alias p = pagos sin filtrar).

Archivos: `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport (git pull --ff-only o worktree nuevo desde origin/main)`

Tests obligatorios:
- git merge-base --is-ancestor 25b24ea HEAD (debe pasar antes del primer commit)
- suite backend completa en verde ANTES de tocar nada, para tener linea base

**2. 1.0.a (previo, bloquea 1.2)** (riesgo medio, depende de: paso 1)

Sustituir los dos whereIn de estados escritos a mano en el lado de la ESCRITURA por PlaClubTeamPayment::ACTIVE_STATUSES. VERIFICADO: el dispatch 'generar pagos ahora' usa ['PEN','PAR','COM'] (PlaClubTeamChargeController.php:739) y el generador de cuota de torneo usa ['PEN','PAR','COM','OVD','PEV'] (PlaTournamentController.php:3518); ninguno incluye AGR ni EXO. Efecto en prod HOY: a un jugador con acuerdo de pago o exonerado se le crea un SEGUNDO cobro del mismo mes. Va antes de 1.2 porque con covers_* eso deja de ser un duplicado invisible y pasa a ser dos filas con el mismo covers_from; y hace imposible cualquier indice unico posterior. Es la misma trampa DEBT_STATUSES/ACTIVE_STATUSES del spec, pero del lado de la escritura.

Archivos: `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Http/Controllers/PlaClubTeamChargeController.php:739`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Http/Controllers/PlaTournamentController.php:3518`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Models/PlaClubTeamPayment.php:125`

Tests obligatorios:
- test_dispatch_no_duplica_cuota_de_jugador_en_acuerdo (jugador con pago AGR del mes -> 'generar pagos ahora' no crea fila)
- test_dispatch_no_duplica_cuota_de_jugador_exonerado (pago EXO del mes -> no crea fila)
- test_cuota_de_torneo_no_duplica_sobre_AGR_ni_EXO
- Reversion: revert de 2 lineas, no toca datos existentes

**3. 1.0.b (previo, bloquea 1.1)** (riesgo bajo, depende de: paso 1)

Arreglar el mapa de frecuencias antes de que la migracion retro lo herede. VERIFICADO contra BasFrequencySeeder.php:15 (orden: 1 Pago Unico, 2 Mensual, 3 Trimestral, 4 Semestral, 5 Anual): PlaClubTeamChargeController.php:839 hace `frequency_id != 5 // 5 = Pago Unico` cuando 5 es ANUAL, y DispatchChargeDialog.jsx:116 y :178 repiten el mismo literal; ademas BasFrequency::FREQUENCY_ANNUAL=3 apunta a Trimestral (BasFrequency.php:14, hoy inerte porque getFrequencyDescription() no se llama en ningun sitio). Regla de la casa a partir de aqui: la recurrencia se decide SIEMPRE por bas_frequencies.is_single_payment / interval_type / interval_count, NUNCA por id ni por constantes del modelo.

Archivos: `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Http/Controllers/PlaClubTeamChargeController.php:839`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Models/BasFrequency.php:14`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/frontend/src/components/charges/DispatchChargeDialog.jsx:116`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/frontend/src/components/charges/DispatchChargeDialog.jsx:178`

Tests obligatorios:
- test_cobro_de_pago_unico_con_due_day_pasado_no_empuja_el_vencimiento_un_mes
- test_el_dialogo_de_despacho_no_pide_mes_para_pago_unico_y_si_para_anual (o al menos snapshot del componente)
- Test que falle si alguien vuelve a escribir un literal frequency_id == N en app/ o en src/

**4. 1.0.c (previo, BLOQUEA el backfill de 1.2)** (riesgo medio, depende de: paso 3)

Unificar el calculo de due_date para que ninguna ruta salte de mes. VERIFICADO: cuatro formulas distintas. El cron (GenerateMonthlyPayments.php:534) y el observer (PlaClubTeamPlayerObserver.php:338) acotan a fin de mes ACTUAL (correcto). ChargeService.php:654 y PlaClubTeamChargeController.php:825 hacen addMonth() si el due_day ya paso -> el pago del mes N queda con due_date del mes N+1. Y el propio dispatch comprueba el duplicado por whereYear/whereMonth(due_date, referenceDate) (:744) contra un due_date que ya salto (:840): generar el 28 crea el pago de agosto venciendo en septiembre y luego septiembre se salta. ESTE ES EL PASO QUE MAS REORDENA LA FASE: el backfill de covers_* (paso 10) lee due_date; si se hace antes de arreglar esto, congela el mes equivocado en un campo que nadie va a volver a mirar.

Archivos: `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Services/ChargeService.php:654`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Http/Controllers/PlaClubTeamChargeController.php:825`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Http/Controllers/PlaClubTeamChargeController.php:744`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Console/Commands/GenerateMonthlyPayments.php:534`

Tests obligatorios:
- test_alta_de_jugador_el_dia_20_con_due_day_5_vence_fin_de_mes_actual_no_mes_siguiente
- test_dispatch_el_dia_28_no_se_salta_el_mes_siguiente
- Test de paridad: las 4 rutas (cron, observer, ChargeService, dispatch) producen el MISMO due_date para el mismo cobro y la misma fecha
- Reversion: revert de codigo; NO toca pagos historicos (solo cambia lo que se cree a partir de ahora)

**5. 1.1.a** (riesgo bajo, depende de: paso 1)

Migracion aditiva: `concept` varchar(20) NULLABLE (NO enum: el spec ya prevé un sexto valor 'agreement' y ampliar un ENUM en MySQL es un ALTER con reescritura; el precedente correcto en la tabla es `apply_to` varchar(20), migracion 2026_02_18_000001) + `concept_needs_review` boolean default 0. Anadir 'concept' a $fillable (PlaClubTeamCharge.php:36) — obligatorio, no cosmetico: sin fillable, `create($request->validated())` lo descarta EN SILENCIO, exactamente como hoy desaparecen late_fee_percentage/late_fee_amount/late_fee_grace_days que el agente IA cree que configura (WriteTools.php:417 escribe columnas que no existen). No anadir cast (es string). Observer/mutator que mantenga is_enrollment_fee = (concept === 'enrollment') como espejo, para que los 9 lectores del booleano (ChargeService::isChargeProratable:288, detectDuplicateEnrollmentFees:1135, PublicEnrollmentController:1808, SimpleDashboardController:994, ClubExportService:350, badges del frontend...) NO haya que tocarlos en Fase 1.

Archivos: `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/database/migrations/ (nueva)`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Models/PlaClubTeamCharge.php:36`

Tests obligatorios:
- test_concept_se_persiste_via_create (guard contra el bug de fillable)
- test_is_enrollment_fee_es_espejo_de_concept_enrollment en create y en update
- Reversion: dropColumn de las 2 columnas; nada mas depende de ellas todavia

**6. 1.1.b** (riesgo bajo, depende de: paso 5)

Que los 5 caminos de creacion escriban concept, ANTES del backfill, o los cobros nuevos nacen sin clasificar mientras el backfill corre. VERIFICADO, inventario cerrado: (1) web/API via StorePlaClubTeamChargeRequest.php:100 + UpdatePlaClubTeamChargeRequest.php:108 (validacion required|in:...) alimentado por useChargeWizard.jsx:602; (2) PlaTournamentController.php:3476 -> concept='event'; (3) WriteTools.php:422 (agente IA de chat) + ToolDefinitions.php:373 (schema del tool); (4) OnboardingToolExecutor.php:433; (5) PaymentAgreementService.php:52 -> concept='agreement'. Mas factory y 5 seeders, TRES de ellos con DB::table()->insertGetId (MultiRoleUserDataSeeder.php:432/608/617) que revientan si la columna nace NOT NULL. Arreglar de paso el late_fee_* fantasma del agente: es el mismo mecanismo y es un bug real del camino que mas cobros crea sin supervision humana. NO TOCAR: ChargesSheetContent.jsx (1362 lineas con UI de is_enrollment_fee en :1210-1259) esta MUERTO, no lo importa nadie.

Archivos: `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Http/Requests/StorePlaClubTeamChargeRequest.php:100`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Http/Requests/UpdatePlaClubTeamChargeRequest.php:108`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Http/Controllers/PlaTournamentController.php:3476`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Services/Assistant/Tools/WriteTools.php:417`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Services/Assistant/ToolDefinitions.php:373`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Services/OnboardingToolExecutor.php:428`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Services/PaymentAgreementService.php:52`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/database/factories/PlaClubTeamChargeFactory.php:14`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/database/seeders/MultiRoleUserDataSeeder.php:432`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/frontend/src/components/charges/hooks/useChargeWizard.jsx:602`

Tests obligatorios:
- Un test por camino de creacion (5) que afirme concept != null y con el valor esperado
- test_el_agente_que_pide_recargo_por_mora_lo_persiste (regresion del late_fee fantasma)
- php artisan db:seed de los 5 seeders sin error

**7. 1.1.c** (riesgo alto, depende de: pasos 3 y 6)

Clasificacion retro por CASCADA ordenada, primera regla que acierta gana, ejecutada primero como comando --dry-run que imprime el reparto y la lista de dudosos, y solo despues como migracion idempotente. Orden OBLIGATORIO (la frecuencia es la ULTIMA senal, no la primera): R0 name='__payment_agreement__' -> agreement; R1 id en pla_tournaments.charge_id o name LIKE 'Torneo:%' -> event; R2 is_enrollment_fee=1 -> enrollment; R3 billing_mode='per_attendance' -> training_fee; R4 nombre uniforme/kit/dotacion -> uniform; R5 nombre torneo/campeonato/viaje/clinic -> event; R6 nombre matricula/inscripcion -> enrollment; R7 nombre mensualidad/cuota/trimestre/semestre -> training_fee; R8 frecuencia con is_single_payment=0 e interval_type='months' -> training_fee; R9 resto -> concept='other' + concept_needs_review=1. POR QUE ESTE ORDEN: verificado en AbaClubSeeder.php:428/435 (espejo de produccion, mapea prod_charge_id) que ABA registro 'Trimestre Categorias Sub 13-18' con frequency 'Pago Unico' y sus matriculas con 'Anual'; la regla del spec 'recurrente mensual -> training_fee, resto -> other' manda los trimestres REALES al cubo equivocado. Correr las regex sobre stripDeletedSuffix (los borrados llevan name_deleted_<ts>) e incluir soft-deleted.

Archivos: `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Console/Commands/ (comando nuevo de clasificacion con --dry-run)`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/database/migrations/ (backfill idempotente, separada de la del paso 5)`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/database/seeders/AbaClubSeeder.php:389`

Tests obligatorios:
- Fixture con los cobros REALES de AbaClubSeeder: los 'Trimestre Categorias Sub X' salen training_fee y las matriculas anuales salen enrollment (el test que caza la trampa de la frecuencia)
- test_el_cobro_sintetico_de_acuerdo_sale_agreement y test_cobro_de_torneo_sale_event
- test_cobro_borrado_con_sufijo__deleted_ts_se_clasifica_por_su_nombre_limpio
- La migracion corre dos veces sin cambiar nada (idempotencia)
- Reversion: UPDATE pla_club_teams_charges SET concept=NULL, concept_needs_review=0 — no se pierde ningun dato preexistente, pero hay que guardar el CSV del dry-run antes de aplicar

**8. 1.1.d** (riesgo medio, depende de: paso 7)

Cerrar la columna: NOT NULL DEFAULT 'other' SOLO cuando SELECT COUNT(*) WHERE concept IS NULL = 0. Migracion aparte, tercera. Nunca en el mismo ALTER que la creacion: en MySQL sobre una tabla con filas eso exige que el backfill ya este hecho, y es justo donde revienta si una regla devuelve NULL. Mantener concept_needs_review como el estado 'sin clasificar' distinto de 'other': si todo lo dudoso cae en 'other', 'other' pierde significado, el aviso 'clasifica tus cobros' no sabe cuando apagarse y el reporte de 1.4 no puede distinguir 'es otro' de 'no lo hemos mirado'.

Archivos: `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/database/migrations/ (tercera migracion)`

Tests obligatorios:
- Guard que falle el deploy si quedan NULL antes del ALTER
- test_crear_un_cobro_sin_concept_lo_deja_marcado_para_revision_y_no_silenciosamente_en_other
- Reversion: volver a nullable (ALTER MODIFY) — reversible sin perdida

**9. 1.2.a** (riesgo medio, depende de: pasos 2 y 4)

Columnas covers_from / covers_to (date, nullable) + covers_reason (varchar, para distinguir 'no aplica' de 'desconocido'), y derivarlas en el hook `creating` de PlaClubTeamPayment.php:23 — que es el UNICO punto por el que pasan los ONCE creadores (ahi ya se resuelve el consecutivo). VERIFICADO: son 11 sitios en produccion, no 5: cron (GenerateMonthlyPayments.php:560), alta de jugador (ChargeService.php:239), observer y cambio de categoria (PlaClubTeamPlayerObserver.php:280 + PlayerService.php:1393), dispatch (PlaClubTeamChargeController.php:845), registro manual (PlaClubTeamPaymentController.php:473), inscripcion publica (PublicEnrollmentController.php:2075), torneo (PlaTournamentController.php:3529), asistencia (AttendancePaymentService.php:112), agente IA (WriteTools.php:971), cuotas de acuerdo (PaymentAgreementService.php:155), backfill (BackfillMissingMonthlyCharges.php:471). Los que SI conocen el periodo lo pasan explicito: cron (mes del club en su timezone), alta prorrateada (covers_from = fecha de INGRESO real, no dia 1 — es la unica forma de que 'monto != mes completo' quede explicado en el dato), dispatch con targetMonth, asistencia (covers_from = covers_to = attendance_date). Extraer la ventana de periodo por frecuencia de GenerateMonthlyPayments::getExistingPayment:465-517 a un helper unico periodo(charge, fecha) -> [from,to]: YA esta bien escrita y ya usa ACTIVE_STATUSES; 1.3 debe reusarla, no reescribirla.

Archivos: `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/database/migrations/ (nueva)`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Models/PlaClubTeamPayment.php:23`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Console/Commands/GenerateMonthlyPayments.php:465`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Services/ChargeService.php:239`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Observers/PlaClubTeamPlayerObserver.php:280`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Services/AttendancePaymentService.php:112`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Services/PaymentAgreementService.php:155`

Tests obligatorios:
- ONCE tests, uno por creador: crear un pago desde cada ruta y afirmar covers_from NOT NULL o covers_reason explicito (este es el test que impide repetir el fallo de 'solo el cron')
- test_el_primer_mes_prorrateado_tiene_covers_from_en_la_fecha_de_ingreso_no_el_dia_1
- test_cobro_por_asistencia_cubre_un_dia_no_un_mes
- test_las_cuotas_de_acuerdo_no_pisan_los_covers_de_los_pagos_AGR_originales
- Reversion: dropColumn de las 3 columnas

**10. 1.2.b** (riesgo alto, depende de: pasos 4 y 9)

Backfill de covers_* con CUATRO reglas y una segunda pasada, no con la regla unica del spec. Regla 1 (mensualidades reales, la unica automatizable): p.covers_from=DATE_FORMAT(due_date,'%Y-%m-01'), covers_to=LAST_DAY(due_date), acotada a f.interval_type='months' AND f.interval_count=1 AND f.is_single_payment=0 AND billing_mode<>'per_attendance' AND is_enrollment_fee=0 AND attendance_id IS NULL AND agreement_role IS NULL AND c.specific_due_date IS NULL. Regla 2: attendance_id NOT NULL -> covers_from=covers_to=attendance_date. Regla 3: matriculas y pagos unicos -> NULL con covers_reason='temporada no modelada' / 'no aplica' (la temporada es Fase 3, §5.4). Regla 4 (SEGUNDA PASADA, despues de la primera): cuotas de acuerdo (agreement_role='installment') heredan min(covers_from)/max(covers_to) de los covered — y solo es seguro si el reporte excluye concept='agreement' en la consulta canonica, no en cada reporte, o el mes queda cubierto DOS veces (la fila AGR original conserva su periodo). EXCLUIR de la regla 1 los pagos con mes(due_date) != mes(created_at): son los que produjeron las formulas rotas del paso 4 y quedarian en un mes ya cubierto. Decision explicita a escribir, no dejar al azar: los soft-deleted SI se backfillean (son historia; el reporte los excluye por su cuenta).

Archivos: `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Console/Commands/ (comando con --dry-run que imprime el reparto por regla)`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/database/migrations/ (backfill idempotente)`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Console/Commands/FixPaymentsNullDueDate.php:69`

Tests obligatorios:
- test_cada_una_de_las_4_reglas_con_su_caso_y_su_contraejemplo
- test_un_mes_con_acuerdo_de_pago_suma_UNA_sola_vez (anti doble cobertura)
- test_pago_con_due_date_del_mes_siguiente_no_se_backfillea_y_queda_con_covers_reason
- La migracion corre dos veces sin cambiar nada
- Reversion: UPDATE SET covers_from=NULL, covers_to=NULL, covers_reason=NULL. ORDEN OPERATIVO: correr payments:fix-null-due-date ANTES del backfill (o retirarlo), porque reescribe due_date con saveQuietly() sin auditoria y dejaria covers_* y due_date desincronizados sin que nadie se entere

**11. 1.3.a (puede ir en paralelo desde el paso 1)** (riesgo alto, depende de: paso 1)

ClubMetricsService con los cubos de DINERO. No depende de 1.1 ni de 1.2: puede arrancar en paralelo. VERIFICADO: 'recaudado' no tiene 3 definiciones vivas sino OCHO, sobre 4 columnas de fecha (reviewed_at, paid_at, payment_date y updated_at) y 2 fuentes de importe (installments APR vs payments.amount_paid): PaymentStatsService.php:59, FinancialReportController.php:228, :352, PlaClubTeamController.php:479, AdvancedTools.php:495, DailyBriefService.php:236, SendWeeklyClubReport.php:58, ReadTools.php:1187. Requisitos de forma: `periodo` y `base` (devengo|caja) como parametros OBLIGATORIOS SIN default (todo el desastre actual son defaults silenciosos: getTotalBilled:144 no acepta periodo y getReceivedAmount:59 si, asi que la cabecera de Pagos pinta una caja mensual junto a una deuda historica y I2 hoy ni siquiera es evaluable); TODOS los cubos derivados de UNA tabla de decision estado x cubo escrita como constante de datos, para que sea imposible que dos metodos discrepen (hoy getTotalBilled excluye AGR/EXO y getReceivedAmount no, asi que su dinero cobrado sigue en el recaudo); baseQuery() privado que use withoutGlobalScope(ClubScope::class) y NUNCA withoutGlobalScopes() sin argumentos (que quita tambien el SoftDeletingScope y ya fallo en 6 sitios); I2 con termino propio para el RECARGO POR MORA desde el primer diseno (facturado + recargos = recaudado + por_cobrar + anulado), porque la mora reescribe remaining_amount sin tocar amount_due y el dia que reviva sobre los 586 vencidos descuadra I2 en todos los clubes a la vez; y B1 ya esta cerrado en origin/main (hook is_canceled = status==='CXL' + migracion 2026_08_04_000100), asi que la consulta canonica mira SOLO status y no arrastra el booleano. Adopcion por DELEGACION: PaymentStatsService conserva su firma publica y por dentro llama al nuevo servicio (los 11 llamadores se benefician sin tocarlos).

Archivos: `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Services/ (ClubMetricsService nuevo)`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Services/PaymentStatsService.php:59`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Http/Controllers/Api/FinancialReportController.php:228`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Http/Controllers/Api/DashboardController.php:23 (borrar o marcar: calcula todo con status 'completed'/'pending' en minuscula, estados que no existen; devuelve cero siempre y no esta enrutado)`

Tests obligatorios:
- test_I2_cuadra_para_un_periodo_con_pagos_en_los_8_estados
- test_dos_consumidores_distintos_dan_el_mismo_numero (multiplicar el patron de DashboardDebtorsConsistencyTest, el unico test 'dos pantallas un numero' del repo)
- PaymentStatsServiceTest (10 casos) sigue verde tras la delegacion
- RESOLVER EXPLICITAMENTE el conflicto con PaymentAgreementNoDoubleCountingTest:74-151 (test_money_received_before_the_agreement_still_counts_as_income): hoy codifica el desfase de I2 como comportamiento esperado. Decidir y reescribir, no borrar
- El self-check en runtime reporta el DESFASE descompuesto por cubo, no un booleano (un check siempre rojo se ignora y luego se borra)

**12. 1.3.b** (riesgo alto, depende de: pasos 9 y 11)

Predicado canonico 'esta cubierto?' + universo canonico de 'jugador activo'. VERIFICADO: hay TRES definiciones incompatibles de 'ya existe pago de este periodo' y las tres usan ACTIVE_STATUSES (bien) pero responden distinto: ChargeService.php:174 no tiene NINGUNA ventana temporal (un pago activo de cualquier epoca lo da por cubierto para siempre), GenerateMonthlyPayments.php:465 usa ventana por due_date segun la frecuencia, PlaClubTeamPaymentController.php:275 usa ventana por created_at. Y hay CUATRO definiciones de 'jugador activo' (PlaClubTeamPlayer NO tiene scope active): status='ACT'; withTrashed()+status!='BOR'; deleted_at IS NULL AND end_date IS NULL AND users.status!='BOR'; deleted_at IS NULL AND status!='BOR'. Fijar UNA (recomendado: status='ACT' + SoftDeletes por defecto via Eloquent + whereHas('categories')) y que las tres implementaciones pasen a llamar al servicio. Ojo con per_attendance: hay DOS predicados incompatibles (el generador y ChargeService/PlayerService/backfill usan whereHas('players'), AttendancePaymentService.php:175 usa appliesToPlayer() con apply_to); el reporte debe usar EL DEL GENERADOR o dira 'sin cuota' de alguien a quien el cron nunca le va a generar nada.

Archivos: `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Services/ChargeService.php:174`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Console/Commands/GenerateMonthlyPayments.php:465`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Http/Controllers/PlaClubTeamPaymentController.php:275`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Models/PlaClubTeamPlayer.php:16`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Services/PlayerService.php:1413`

Tests obligatorios:
- test_las_tres_rutas_responden_lo_mismo_para_el_mismo_jugador_cobro_y_periodo
- test_un_pago_unico_de_2024_NO_deja_al_jugador_cubierto_en_2026 (hoy si; es exactamente el modelo con el que ABA falsifico sus trimestres)
- test_jugador_en_AGR_cuenta_como_cubierto y test_jugador_en_EXO_cuenta_como_cubierto (ACTIVE_STATUSES, jamas DEBT_STATUSES)
- test_becado_al_100_con_amount_due_0_cuenta_como_CUBIERTO (R10: cubierto = existe fila en ACTIVE_STATUSES, NUNCA monto > 0; hoy ChargeService.php:223 descarta la fila si el importe es 0 y el cron NO, asi que el mismo becado esta cubierto o descubierto segun que generador lo toco)
- El cron y el reporte no se contradicen: correr el generador y el reporte sobre el mismo fixture y comprobar que el reporte no lista a nadie a quien el cron no le vaya a generar nada

**13. 1.3.c** (riesgo medio, depende de: paso 11)

Cerrar las fugas que quedan y poner el trinquete, o el servicio canonico convive con los literales y no cambia nada (que es exactamente lo que le paso a PaymentStatsService: existe hace meses, esta bien documentado, y aun asi conviven 7 definiciones mas de recaudado). CUATRO fugas de soft delete VERIFICADAS EN origin/main (no en el arbol atrasado): ReadTools.php:1655 getDebtorsSummary — alias p=pagos, pl=jugadores, tiene whereNull('pl.deleted_at') y NO p.deleted_at: es EXACTAMENTE el patron trampa del encargo, y es la lista de deudores que el agente le lee al director; ReadTools::getPendingPayments — la lista (desde :259) sin deleted_at contra sus totales que si filtran, en la MISMA respuesta; FamilyTools lista de pagos de la familia (sin deleted_at) vs sus totales (:314, si filtran) — un padre ve un cobro que el club borro; PlaClubTeamChargeController::playerChargeDetail:1223 con withoutGlobalScopes() sin whereNull. Y CUATRO whereIn de estados a mano en el agente: AdvancedTools.php:509/:725/:745 y PlayerTools.php:71 en SQL crudo; el peor es AdvancedTools.php:779 (queryActiveGoodStanding) que filtra ['OVD','PEN'] SIN 'PAR' -> quien abono a medias se reporta al director como 'al dia'. Trinquete estatico (el repo ya tiene el mecanismo, api:contract-check con baseline en cero) que prohiba DB::table('pla_club_teams_payments') y whereIn(...'status'...) fuera del namespace de metricas, baseline congelado y prohibicion de crecer.

Archivos: `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Services/Assistant/Tools/ReadTools.php:1655`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Services/Assistant/Tools/ReadTools.php:259`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Services/Assistant/Tools/FamilyTools.php:269`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Http/Controllers/PlaClubTeamChargeController.php:1223`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Services/Assistant/Tools/AdvancedTools.php:779`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Services/Assistant/Tools/PlayerTools.php:71`

Tests obligatorios:
- DeletedPaymentsNeverCountTest crece con un caso por cada una de las 4 fugas
- test_jugador_con_pago_parcial_NO_aparece_como_al_dia (regresion de AdvancedTools:779)
- test_la_lista_y_el_total_de_getPendingPayments_cuadran
- El trinquete falla si alguien anade un DB::table de pagos o un whereIn de status fuera del namespace

**14. 1.4** (riesgo bajo, depende de: pasos 8, 10 y 12)

Reporte de cobertura con motivos. Solo lectura, riesgo bajo, pero solo tiene sentido despues de 1.1+1.2+1.3.b. Endpoint GET /api/pla_club_teams/{club}/financial-reports/coverage dentro del grupo existente (routes/api.php:1072, middleware module.access:reports), con authorize() EXPLICITO — el controlador actual comprueba roles a mano y no tiene Policy, y la regla de la casa para endpoints nuevos es authorize(). UN solo endpoint que devuelva tiles + filas, para poder auto-verificar I3 sobre su propia respuesta. Universo: jugador ACT + con categoria (via relacion Eloquent, NO leftJoin sobre la pivote: pla_club_teams_category_player no tiene club_id ni deleted_at ni status, y un jugador cuya unica categoria esta BORRADA cuenta como 'con categoria' si se hace con DB::table) + no excluido + no per_attendance (predicado del GENERADOR) + no exento total. Pagos considerados: concept='training_fee' AND agreement_role IS NULL, en ACTIVE_STATUSES. Cuatro motivos con PRECEDENCIA escrita y testeada (sugerido 4<1<3<2, porque la maqueta M3 pinta a Helen como 'cancelado sin reemplazo' teniendo modalidad sin definir): (1) nunca facturado — mira TODA la historia incluyendo CXL y soft-deleted, o un cancelado se clasifica mal aqui; (2) periodo vencido — necesita covers_*; (3) cancelado sin reemplazo — los datos YA existen y son fiables de aqui en adelante (cancelPayment escribe canceled_at/canceled_by/cancellation_reason y payments:clean-deleted esta COMENTADO en el scheduler, verificado en routes/console.php:165, ademas con candado de dinero: amount_paid>0 nunca se destruye), pero NO para los 172 CXL historicos que solo tienen el motivo en comments; (4) sin modalidad — NO TIENE DATOS en Fase 1. Cuarto cubo explicito 'periodo desconocido' que no cuenta ni como cubierto ni como fuga. Bloque HERMANO y rotulado aparte 'activos que nunca han pagado' (predicado de doble senal: SUM(amount_paid)=0 Y ningun installment APR, porque bulkMarkPaid pone COM sin installment y los pagos legacy no tienen installments), que NUNCA suma al tile de fuga o I3 se rompe. Frontend: 4a pestana en FinancialReportsPage.jsx:551, claves i18n financial.* en el namespace payments (que es DEFAULT en esa pagina).

Archivos: `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Http/Controllers/Api/FinancialReportController.php:34`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/routes/api.php:1072`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/frontend/src/pages/dashboard/FinancialReports/FinancialReportsPage.jsx:551`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/frontend/src/hooks/financialReports/index.js:23`

Tests obligatorios:
- test_un_jugador_con_acuerdo_no_sale_como_descubierto y test_un_jugador_exonerado_no_sale_como_descubierto (el test que impide copiar DEBT_STATUSES)
- test_becado_al_100_por_ciento_no_sale_como_descubierto (R10)
- test_la_suma_de_las_filas_es_igual_al_tile_de_fuga (I3, sobre la propia respuesta del endpoint)
- test_precedencia_de_los_4_motivos con un jugador que cumple tres a la vez
- test_lo_previsualizado_en_la_columna_Esperado_es_exactamente_lo_que_crea_la_accion_Generar_cuota (I6) — ojo: calculatePaymentAmount resuelve el descuento con PlaClubTeamDiscount::find(), que aplica SoftDeletes, asi que un descuento borrado devuelve null en silencio y factura la cuota COMPLETA a un becado (ChargeService.php:556/563)
- test_de_permisos: contador solo lectura, entrenador 403

**15. 1.5** (riesgo medio, depende de: paso 14)

Senal persistente + aviso en inicio. El daily brief NO es el detector: DailyBriefService.php:29 recalcula 12 items en memoria dentro de un Cache::remember de 15 min por club+locale y no persiste NADA (el unico estado es AIAutonomousAction.reported_at:199); se calcula cuando alguien abre el home, asi que por si solo nunca habria cazado a ABA en marzo. Copiar el patron que SI tiene cron, persistencia y ciclo de vida: PlatformSignalService::persist:239 (idempotente, conserva detected_at si la condicion sigue, marca resolved si desaparece) + cron en routes/console.php:216. Comando revenue:detect-coverage-gaps a las 07:00, DESPUES de app:generate-monthly-payments, con withoutOverlapping()->onOneServer(); tabla propia con status active/resolved, count, reasons[], detected_at, last_notified_at, snoozed_until; umbrales en config/revenue_assurance.php (precedente explicito: config/platform_signals.php); escalado por ANTIGUEDAD del periodo vencido, que es lo que dispara en marzo sin esperar a volumen. El brief solo LEE la fila. FALLO SILENCIOSO NUMERO UNO: OwnerTodayHome.jsx:182 filtra los items contra la lista blanca WARNING_META (:63, 4 tipos); stale_incapacity y no_proof_uploaded llevan meses construyendose con severity 'warning' en backend SIN renderizarse en ninguna parte de la web — grep de esos tipos en frontend/src no devuelve nada. En movil SI se veria (filtra por severity, mobile/app/(app)/(tabs)/index.jsx:131), asi que el QA movil puede dar verde con la web muerta. Anadir tambien el caso al match de sugerencia (:316 en, :323 es) o la senal no tendra call-to-action. NADA de catch mudo: las 4 anomalias actuales terminan en '// Silently skip' (:248, :265, :280, :296) y la anomalia de recaudo estuvo muerta meses sin que nadie lo notara. Arreglar de paso esa anomalia, que usa withoutGlobalScopes() sin whereNull('deleted_at') (suma pagos borrados) y compara por updated_at (DailyBriefService.php:236-243) — que ademas se movera en masa el dia que reviva la mora.

Archivos: `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Console/Commands/ (revenue:detect-coverage-gaps nuevo)`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/database/migrations/ (tabla de senales de club)`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/routes/console.php:216`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Services/Assistant/DailyBriefService.php:236`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/saas_sport/app/Http/Controllers/Api/V1/ClubAssistantController.php:516`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/frontend/src/components/home/OwnerTodayHome.jsx:63`, `/Users/miguelcano/Desktop/todo/Widdo/desarrollo/frontend/src/i18n/locales/es/home.json`

Tests obligatorios:
- Test de FRONT (o walkthrough del harness npm run test:ux) que verifique que la tarjeta APARECE en la web — sin esto la senal se construye y no la ve nadie
- test_no_suena_entre_el_dia_1_y_el_due_day (ventana de silencio: el cron genera cuando son las 9 AM en el timezone del club, asi que entre el corte de mes y esa ejecucion TODO el club esta tecnicamente descubierto)
- test_no_re_anuncia_si_el_conteo_no_empeora
- test_la_senal_lee_la_consulta_canonica_de_1.3_y_no_repite_el_whereIn
- Claves i18n en es/en/pt-BR + los dos ternarios en PHP (el servicio solo produce es/en, ClubAssistantController.php:522 solo acepta esos dos locales: un club BR ve titulo en portugues y razon en espanol)
- Test de rol: el endpoint /v1/assistant/daily-brief NO verifica rol hoy (routes/api.php:722+1548, el club sale de current_club_id), asi que anadir cobertura amplia una fuga existente. Filtrar dentro del servicio y meter el rol en la clave de cache


### 🔴 Decisiones que necesita Miguel

> ⚠️ **Reconciliadas el 3-ago.** Estas D1-D6 se solapaban con las preguntas abiertas que el
> spec ya traía en su §14 (D6 era literalmente la pregunta 4). **La lista viva es la del spec**
> (`2026-07-30-cobros-v2-revenue-assurance.md` §14, ahora P1-P9). Lo de abajo se conserva por
> el detalle de opciones y el razonamiento, pero no preguntes dos veces.

**D1. ¿El descuento persistente por jugador (player_discount) sube a la Fase 1?**

- Sube entero a Fase 1: campo persistente + UI, antes del reporte de cobertura
- Sube solo la mitad barata: que el cron persista discount_amount/discount_name como ya hace ChargeService, sin campo nuevo
- No sube: el reporte de cobertura sale en Fase 1 con los becados mal marcados y se arregla en Fase 2

> **Recomendación:** Sube la mitad barata AHORA (paso 9) y el campo persistente en Fase 2. Motivo tecnico medido: el cron escribe amount_due y discount_id pero NO discount_amount ni discount_name (GenerateMonthlyPayments.php:563), mientras ChargeService.php:587 si lo escribe. Hoy el unico marcador utilizable para 'vale cero por beca' es amount_due=0 AND discount_id IS NOT NULL, y es fragil: si discount_id es NULL y el monto es 0, no hay forma de distinguir 'becado' de 'error'. Con solo persistir esos dos campos, R10 queda cubierto sin campo nuevo ni migracion de datos.

> **Por qué importa:** En un club hay 32 jugadores con descuento aplicado a mano y solo aciertan el 71% de los meses. Sin resolver esto, el reporte de cobertura marca mal a los becados y lo primero que ve el director es una lista con 9 nombres que no deberian estar ahi — y un reporte que se equivoca la primera vez no se vuelve a abrir. Peor: la accion 'Generar cuota' del reporte puede facturarle la cuota COMPLETA a un becado, porque calculatePaymentAmount resuelve el descuento con PlaClubTeamDiscount::find() que aplica SoftDeletes y devuelve null en silencio si el descuento fue borrado (ChargeService.php:556/563).

**D2. ¿Sube 2.1 (payment_modality: quien paga mensual, quien trimestral) a la Fase 1, o el reporte sale con 3 motivos de 4?**

- Sube 2.1 a Fase 1: el reporte nace con los 4 motivos
- El reporte sale con 3 motivos + un cubo rotulado 'periodo desconocido', y 2.1 va en Fase 2
- El reporte espera a 2.1 y no sale en Fase 1

> **Recomendación:** Sale con 3 motivos y el cubo 'periodo desconocido' rotulado. Meter 2.1 en Fase 1 la alarga y arrastra decisiones de producto (quien fija la modalidad, el club o el padre) que no estan tomadas.

> **Por qué importa:** Verificado por grep: payment_modality no existe en ningun modelo ni migracion del backend. El unico sustituto vivo en produccion son las 162 exenciones con motivo automatico 'jugador asignado a cobro alternativo' (160 de ABA = 40 jugadores x 4 cobros): la exencion se esta usando como si fuera la modalidad. Eso obliga a una segunda decision inmediata: si el reporte excluye 'exentos' a secas, deja fuera EXACTAMENTE a los 40 jugadores del trimestre vencido, que son la poblacion que el reporte existe para vigilar. Recomiendo excluir solo la exencion TOTAL (charge_id NULL) y tipos full/scholarship, y tratar la exencion por cobro alternativo como cobertura por otra cuota, verificada contra su periodo.

**D3. El dinero que un padre pago ANTES de entrar en un acuerdo de pago o antes de ser exonerado, ¿sigue contando como recaudado del mes en que entro?**

- Si: caja es caja, la plata entro ese mes y ahi se queda (comportamiento actual)
- No: si el pago se fue a AGR o EXO, su dinero sale tambien del recaudado (simetria con facturado)
- Si, pero se muestra como linea aparte 'cobrado antes del acuerdo' en la tarjeta de como cuadra todo

> **Recomendación:** Opcion 1 (caja es caja) + opcion 3 para explicarlo. Cambiar la caja historica para cuadrar un invariante es peor que el descuadre.

> **Por qué importa:** Es una decision de producto disfrazada de bug: getTotalBilled excluye AGR/EXO pero getReceivedAmount no, asi que un pago parcial de $40.000 que despues entra en acuerdo sale de facturado y sus $40.000 siguen en recaudado. Hay un test que ya codifica el comportamiento actual como esperado (PaymentAgreementNoDoubleCountingTest: 'test_money_received_before_the_agreement_still_counts_as_income'). Hoy hay 0 acuerdos en produccion, asi que solo aplica a las 162 exenciones — el momento de decidirlo es AHORA, antes de que existan acuerdos.

**D4. ¿Quien clasifica los cobros que la migracion retro no sabe clasificar: el club en una pantalla, o Widdo por su cuenta?**

- Widdo los mete todos en 'other' y no molesta a nadie
- Quedan marcados 'sin clasificar' y el club ve un aviso hasta que los revise (una vez, ~5 minutos por club)
- Widdo los clasifica y avisa al club solo si el reporte de cobertura sale raro

> **Recomendación:** Opcion 2. Es la unica que permite apagar el aviso con criterio y la unica que distingue 'es realmente otro' de 'no lo hemos mirado'.

> **Por qué importa:** Si todo lo dudoso cae en 'other', 'other' pierde significado y el reporte de cobertura no puede excluir lo no clasificado del calculo. Los casos ambiguos que quedaran ahi son reales: el seguro deportivo anual, los pagos unicos con nombre neutro, los cobros ocultos que no son torneo ni acuerdo. Cuesta al club una pasada de 5 minutos y a cambio el numero de fuga es defendible.

**D5. ¿El contador ve el aviso de cobertura? ¿Y el entrenador deja de ver los conteos financieros que ve hoy?**

- El aviso solo para owner y admin (como esta hoy la web)
- Owner, admin y contador — el contador es el rol financiero y es quien mas lo usaria
- Ademas: cerrar de paso la fuga que ya existe (el brief no verifica rol)

> **Recomendación:** Owner + admin + contador para el aviso, y cerrar la fuga del entrenador en el mismo commit.

> **Por qué importa:** Verificado: AccountantTodayHome.jsx:92 usa el brief SOLO para el saludo y el resumen — el contador no ve ningun item de warning hoy, asi que si no se decide, el rol financiero es el unico que no se entera. Y el endpoint /v1/assistant/daily-brief no verifica rol en absoluto: el club sale de current_club_id, asi que HOY un entrenador ya recibe conteos de morosos y montos pendientes. Anadir cobertura le daria ademas la lista de a quien no se le esta cobrando.

**D6. ¿El aviso suena siempre que haya un jugador descubierto, o solo a partir de un umbral?**

- Siempre, aunque sea 1 jugador (maxima sensibilidad, riesgo de ruido)
- Solo a partir de un umbral de volumen (p.ej. 3 jugadores o 5% de activos)
- Umbral de volumen PARA lo del mes, pero cualquier jugador con 2+ periodos vencidos dispara solo

> **Recomendación:** Opcion 3. El escalado por antiguedad es lo que habria cazado a ABA en marzo; el umbral de volumen es lo que evita que el aviso se vuelva ignorable.

> **Por qué importa:** El cron genera cuando son las 9 AM en el timezone de cada club, asi que entre el corte de mes y esa ejecucion TODO el club esta tecnicamente descubierto: un aviso sin ventana de silencio grita 104 jugadores cada dia 1 y en dos meses nadie lo mira. Pero un umbral solo de volumen nunca habria disparado con los trimestres de ABA, que empezaron de uno en uno. Los numeros concretos (3 jugadores, 5%) son hipotesis mias: hay que medir la serie de enero a julio en ABA antes de fijarlos.


### Riesgos principales

- **Arrancar la Fase 1 en el arbol local `saas_sport/` en vez de en origin/main** → VERIFICADO: HEAD local 6e4fcaa esta 10 commits POR DETRAS de origin/main 25b24ea, con `git log origin/main..HEAD` vacio (fast-forward limpio). Sin actualizar, el servicio canonico nace sin el fix de los 5 sitios que contaban borrados como deuda, sin el retiro del borrado y sin la migracion 2026_08_04_000100_sync_is_canceled_with_status — y volveria a resolver B1 desde cero. Gate: `git merge-base --is-ancestor 25b24ea HEAD` antes del primer commit. Efecto colateral util: la trampa que el frente 'cobertura' reporto en PaymentStatsService.php:262 YA esta arreglada arriba; no gastar trabajo ahi.
- **Backfillear covers_* antes de arreglar las dos formulas de due_date que saltan de mes** → ChargeService.php:654 y PlaClubTeamChargeController.php:825 hacen addMonth() cuando el due_day ya paso: el pago del mes N tiene due_date del mes N+1. La regla del spec 'mensual -> mes de su due_date' les asigna el mes equivocado y crea a la vez un duplicado falso y un hueco falso. Y a diferencia de un bug de lectura, esto queda CONGELADO en una columna que nadie va a volver a revisar. Orden obligatorio: arreglar las formulas (paso 4) -> medir cuantos pagos vivos tienen mes(due_date) != mes(created_at) -> excluir esos del backfill automatico con covers_reason -> backfillear el resto.
- **Clasificar los cobros por frecuencia (la regla que el spec propone) y mandar los trimestres reales de ABA a 'other'** → La frecuencia MIENTE y esta escrito en el repo: AbaClubSeeder.php:428/435 (espejo de produccion, mapea prod_charge_id) registra 'Trimestre Categorias Sub 13-18' con frequency_id = Pago Unico, y las matriculas con Anual. La frecuencia debe ser la ULTIMA senal de la cascada, detras de agreement, event, is_enrollment_fee, per_attendance y nombre. Ademas hay dos trampas heredadas que NO hay que copiar: PlaClubTeamChargeController.php:839 y DispatchChargeDialog.jsx:116/178 hardcodean frequency_id != 5 creyendo que 5 es Pago Unico cuando 5 es Anual, y BasFrequency::FREQUENCY_ANNUAL=3 apunta a Trimestral. Regla: la recurrencia se lee SIEMPRE de bas_frequencies.is_single_payment/interval_type/interval_count.
- **Escribir covers_* solo en el cron y dejar diez creadores poniendo NULL** → Son ONCE rutas de creacion en produccion, no una: cron, alta de jugador, observer/cambio de categoria, dispatch, registro manual, inscripcion publica, torneo, asistencia, agente IA, cuotas de acuerdo y el comando de backfill. Si solo escribe el cron, el reporte de cobertura marcara como 'nadie le genero el cobro' a quien pago por inscripcion publica o por alta manual — y es irreversible sin otro backfill. Derivar covers_* en el hook `creating` de PlaClubTeamPayment.php:23, que es el UNICO punto por el que pasan los once, y once tests, uno por ruta.
- **Copiar DEBT_STATUSES en el reporte de cobertura (la trampa que el spec avisa, y que el QA no va a cazar)** → Es el patron dominante del repo (20+ usos frente a ~10 de ACTIVE_STATUSES) y el vecino natural del reporte usa el equivocado. Con 0 acuerdos en produccion HOY, un jugador en AGR no existe para el QA y el fallo solo aparece en prod cuando alguien firme el primer acuerdo. La decision de estados debe vivir UNICAMENTE en la consulta canonica de 1.3 — el reporte no arma su propio whereIn — mas dos tests con nombre explicito (test_un_jugador_con_acuerdo_no_sale_como_descubierto, test_un_jugador_exonerado_no_sale_como_descubierto) y el trinquete estatico que prohiba whereIn de status fuera del namespace de metricas. Precedente de que la constante publica NO basta: AdvancedTools.php:779 filtra ['OVD','PEN'] sin 'PAR' y por eso el agente reporta 'al dia' a quien abono a medias.
- **Construir la senal de 1.5 en el backend y que no la vea nadie en la web** → OwnerTodayHome.jsx:182 filtra los items del brief contra la lista blanca WARNING_META (:63, 4 tipos). Prueba viva: stale_incapacity y no_proof_uploaded se construyen con severity 'warning' en el backend y NO se renderizan en ninguna parte de la web; en movil SI (filtra por severity), asi que un QA movil da verde con la web muerta. La entrada en WARNING_META, las claves i18n es/en/pt-BR y el caso en los dos match de sugerencia van en el MISMO commit que el backend, con un test de front que verifique que la tarjeta aparece.
- **Que el servicio canonico se introduzca 'como opcion' y los 8 sitios viejos sigan vivos** → Ya paso: PaymentStatsService existe hace meses, esta bien documentado, y conviven 7 definiciones mas de recaudado sobre 4 columnas de fecha distintas y 4 whereIn de estados a mano. Adopcion por DELEGACION (PaymentStatsService conserva su firma publica y por dentro llama al nuevo servicio: los 11 llamadores se benefician sin tocarlos) + trinquete estatico con baseline congelado. Y periodo y base (devengo|caja) como parametros OBLIGATORIOS sin default: todo el desastre actual son defaults silenciosos.
- **Que I2 nazca roto el dia que se arregle la mora** → Las tres magnitudes suman COLUMNAS distintas (amount_due / remaining_amount / installments.amount_paid) y la mora escalonada reescribe remaining_amount sin tocar amount_due. Encenderla sobre los 586 vencidos descuadra I2 por el importe exacto de los recargos, en todos los clubes el mismo dia, y el self-check empezara a disparar a Sentry en masa. El recargo entra como termino propio de la ecuacion desde el primer diseno (facturado + recargos = recaudado + por_cobrar + anulado) aunque hoy valga 0, y el self-check reporta el DESFASE descompuesto por cubo, no un booleano.

### Preguntas para producción

- BLOQUEANTE ANTES DE EMPEZAR: ¿produccion esta en origin/main (25b24ea) o mas atras? ¿Corrio ya la migracion 2026_08_04_000100_sync_is_canceled_with_status? Verificacion: SELECT COUNT(*) FROM pla_club_teams_payments WHERE (status='CXL') <> (is_canceled=1) — debe dar 0. Si no ha corrido, B1 sigue abierto y el paso 11 cambia.
- SELECT id, name FROM bas_frequencies ORDER BY id — verificacion directa del mapa ID->nombre. TODO el codigo hardcodea 1 y 5 asumiendo el orden del seeder, pero los IDs son autoincrementales, no declarados. Si prod difiere, la clasificacion retro y el bug de frequency_id!=5 cambian de gravedad.
- Volcado completo para calibrar la cascada R0-R9 en seco: SELECT id, club_id, name, frequency_id, billing_mode, is_enrollment_fee, is_hidden, apply_to, status, deleted_at FROM pla_club_teams_charges. Sin esto las regex del paso 7 son adivinanzas. Incluir el desglose de cuantos cobros vivos hay realmente: total, is_hidden=1, name LIKE 'Torneo:%', name='__payment_agreement__' — el 35 del spec puede o no incluirlos, porque el index() del controlador no filtra is_hidden.
- ¿Cuantos pagos vivos tienen el mes de due_date distinto del mes de created_at, por club? Es EL numero que decide si el backfill de covers_* se puede automatizar o si hay que dejar media base en 'periodo desconocido'. Tambien mide cuanto se mueven las cifras del club al pasar de filtrar por created_at a filtrar por covers_from.
- ¿Cuantos pagos vivos tienen due_date NULL, por club, y desde cuando? Son los que ninguna regla de backfill resuelve y que hoy solo tapan tres fallbacks por created_at. Y en paralelo: ¿cuantos cuelgan de cobros con specific_due_date NOT NULL y frecuencia mensual? Ahi todos los meses comparten el mismo due_date y la regla del backfill los apilaria en un solo mes.
- Distribucion de pagos vivos por: frecuencia del cobro, billing_mode, is_enrollment_fee=1, attendance_id NOT NULL, agreement_role NOT NULL. Es el reparto exacto entre las 4 reglas de backfill del paso 10, y dice de antemano cuanto queda en NULL.
- EL ABANICO: las 8 definiciones de recaudado lado a lado, para club 4 (ABA) y club 14, julio 2026 — D1 reviewed_at/APR, D2 paid_at/APR, D3 paid_at/APR sin deleted_at, D4 COM/payment_date, D5 COM/payment_date por DB::table, D6 COM/updated_at, D7 COM/payment_date semanal, D8 amount_paid por created_at. En pesos. Es el argumento entero de la tarea 1.3 y sin el nadie va a aceptar que se muevan cifras en pantallas vivas.
- ¿Cuanto dinero hay en installments APR cuyo pago esta hoy en CXL, AGR o EXO? SELECT p.status, COUNT(*), SUM(i.amount_paid) ... WHERE i.status='APR' AND p.status IN ('CXL','AGR','EXO') AND p.deleted_at IS NULL GROUP BY p.status. Es el tamano exacto de la rotura de I2 y el input de la decision de Miguel sobre caja vs simetria.
- Con la definicion de cobertura del paso 12 (ACTIVE_STATUSES, jugador ACT + con categoria + sin exencion total/per-attendance/excluido): ¿cuantos jugadores quedan descubiertos HOY por club? Y la MISMA cuenta con DEBT_STATUSES: la diferencia entre las dos cifras es el tamano exacto de la trampa que el recuadro rojo del spec viene a evitar, y sirve de test de regresion con datos reales.
- Serie mensual de ese conteo para ABA de enero a julio 2026. Sin ella NO se puede afirmar que el umbral propuesto en 1.5 habria disparado en marzo — que es la promesa entera de la tarea.
- Numero de jugadores activos con categoria por club calculado con las CUATRO definiciones que conviven (status='ACT'; withTrashed+status!='BOR'; deleted_at NULL AND end_date NULL AND users.status!='BOR'; deleted_at NULL AND status!='BOR'). Si divergen, el reporte cambia de tamano segun quien lo escriba y hay que zanjarlo ANTES del paso 12.
- De las 162 exenciones: cuantas con charge_id NULL (total) vs por cobro; cuantas is_active=1 con end_date ya pasada (las que el backfill respeta y el cron no, porque BackfillMissingMonthlyCharges.php:282 ignora las fechas); y a cuantos jugadores distintos afectan. Sin ese numero no se puede decidir si 'exento' excluye del reporte o entra como cobertura por otra cuota.
- ¿Cuantos pagos ACTIVE tienen amount_due=0, y de esos cuantos con discount_id NOT NULL (becado identificable) vs NULL (indistinguible)? Y al reves: ¿cuantos jugadores tienen hoy un descuento del 100% y NO tienen fila del mes en curso? Ese segundo numero es el hueco real de R10 y el input de la decision sobre player_discount.
- ¿Cuantas filas de pla_club_teams_discount_player apuntan a un descuento con deleted_at NOT NULL? Cada una es un becado al que la accion 'Generar cuota' del reporte le facturaria la cuota COMPLETA, en silencio.
- ¿Existe hoy algun (player_id, charge_id, mes de due_date) con mas de una fila activa en ACTIVE_STATUSES? Si los hay, no se puede crear ningun indice unico sobre covers_from sin limpiarlos antes — y son el rastro de los guards con estados literales del paso 2.
- Distribucion de pagos vivos por estado y club (PEN/PAR/COM/OVD/PEV/AGR/EXO/CXL), excluyendo Upss y Sutagaos. Concretamente cuantos EXO y cuantos AGR por club: dimensiona el falso positivo del reporte y calibra el ruido de la senal.
- Pagos soft-deleted con amount_paid>0 o con installments APR: cuantos y cuanto. Es lo que las 4 fugas que quedan (ReadTools.php:1655, ReadTools::getPendingPayments, FamilyTools, ChargeController.php:1223) siguen mostrandole al director y a los padres.
- ¿Cuantos pagos tienen hoy late_fee > 0 o remaining_amount > amount_due? Deberia ser 0 (mora muerta). Si no lo es, I2 ya esta descuadrado por recargos ANTES de tocar nada.
- Distribucion de due_day de los cobros training_fee ACT por club: define la ventana de silencio del aviso. Si un club tiene due_day 25, el aviso no puede sonar el dia 2.
- Frecuencia real con la que cada owner abre el home. Si es diaria, el brief on-request casi bastaria; si es semanal, el cron con persistencia del paso 15 es obligatorio y no negociable.
- IDs de club de Upss y Sutagaos y como se excluyen (columna de config del club o lista en config/, NUNCA hardcode en la consulta).